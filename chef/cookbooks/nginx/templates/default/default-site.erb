server {
  listen   80;

  access_log  <%= node[:nginx][:log_dir] %>/default.access.log;

  location / {
    include uwsgi_params;
    uwsgi_pass unix:///tmp/<%= node[:nginx][:app_name] %>.sock;
  }
}
